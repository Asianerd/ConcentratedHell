using System;
using System.Collections.Generic;
using System.Text;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;

namespace ConcentratedHell.Combat.Projectiles
{
    /*
    class Amongus:Projectile
    {
        public Amongus(Vector2 position, float direction):base(Type.Amongus, 5f, 20f, position, direction)
        {

        }

        public override void Draw()
        {
            Main.spriteBatch.Draw(sprite, position, null, Color.White, direction + rotation, spriteOrigin, 10f, Math.Abs(direction) >= (Math.PI / 2f) ? SpriteEffects.FlipVertically : SpriteEffects.None, 0f);
        }
    }*/
}
