using Microsoft.Xna.Framework;
using System;
using System.Collections.Generic;
using System.Text;

namespace ConcentratedHell.Combat.Weapons
{
    /*class Amongus_Shotgun:Weapon
    {
        public Amongus_Shotgun() : base(Type.Amongus_Shotgun, Projectiles.Projectile.Type.Amongus, 100f, 1f, Ammo.Type.Shell, new GameValue(0, 15, 1))
        {

        }

        public override void Fire(Vector2 origin)
        {
            base.Fire(origin);

            *//*for (int i = 0; i <= 5; i++)
            {
                var x = new Projectiles.Amongus(origin, );
            }*//*
            var x = new Projectiles.Amongus(origin, Cursor.Instance.playerToCursor);
        }
    }*/
}
